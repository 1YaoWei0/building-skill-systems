Please help me refactor an existing skill repository.

Current state:
- there is a `skills/` directory with 6 skill files
- there is no bootstrap skill
- no clear distinction between workflow skills and utility skills
- no tests
- no hooks
- no install docs
- some users already depend on the current skill names

What I want:
- clearer repository architecture
- a real workflow model
- a better onboarding/bootstrap path
- compatibility with existing names if possible
- a staged migration plan instead of a rewrite-from-scratch answer
